<%= simple_form_for @cocktail do |f| %>
  <div class="form-group">
    <%= f.input :name, label: 'Name', placeholder: 'George' %>
    <%= f.input :photo %>
    <%= f.input :photo_cache, as: :hidden %>
  <%= f.submit %>
<% end %>
